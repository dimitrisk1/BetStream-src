apiVersion: v1
kind: Service
metadata:
  name: {{ include "betstream-chart.fullname" . }}
  labels:
    {{- include "betstream-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "betstream-chart.selectorLabels" . | nindent 4 }}
