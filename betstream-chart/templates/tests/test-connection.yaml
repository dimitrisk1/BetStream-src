apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "betstream-chart.fullname" . }}-test-connection"
  labels:
    {{- include "betstream-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "betstream-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
